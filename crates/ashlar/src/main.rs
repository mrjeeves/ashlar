//! CLI. Implemented by the CLI agent — this scaffold only pins the shape so
//! parallel work compiles. Replace this file whole.
//!
//! Commands shipped this increment: check [--human] [path], fix [path],
//! build [path]. Commands that do not work yet do not exist: unknown
//! commands get a usage error listing what is real.

fn main() {
    todo!("cli agent: implement per reference §11")
}
